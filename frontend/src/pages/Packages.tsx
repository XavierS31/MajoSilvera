import { Link } from 'react-router-dom'
import { PageHero } from '@/components/PageHero'
import { FloralLotus } from '@/components/SvgIcons'
import { treatmentPackages } from '@/data/packagesData'

export function Packages() { return <main className="page-shell"><PageHero eyebrow="Planes de tratamiento" title={<>Resultados que se construyen<br/><em>con continuidad.</em></>}><p>Paquetes diseñados para reservar tu proceso y reducir el valor de cada sesión.</p></PageHero><section className="section cream"><FloralLotus className="motif top-right"/><div className="container"><div className="package-grid">{treatmentPackages.map((item) => <article className="package-card" key={item.id}><small>{item.savings}</small><div><h2>{item.name}</h2><p>{item.sessions}</p></div><div className="package-price">{item.price}<del>{item.originalPrice}</del></div><Link className="package-contact" to={`/contacto?service=${encodeURIComponent(item.name)}`}>Contacto</Link></article>)}</div><div className="notice" style={{ marginTop: '1px' }}><strong>Punción seca · 5 sesiones — $595.000</strong><br/><span className="soft-dark">Rehabilitación funcional: armamos el paquete según tu diagnóstico y plan de tratamiento.</span></div><p className="eyebrow" style={{ textAlign: 'center', marginTop: '2rem' }}>Vigencia · 3 meses desde la primera sesión</p></div></section></main> }
